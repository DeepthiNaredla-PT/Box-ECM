server {
    listen 80;
    server_name _;

    client_max_body_size 30M;

    root /usr/share/nginx/html;

    # React SPA fallback
    location / {
        try_files $uri $uri/ /index.html;
    }
}
